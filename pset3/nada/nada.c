#include<stdio.h>
#include<cs50.h>
#include<string.h>

// int SIZE = 2;
// int arr[] = {1,2,3,4,5,6};

char input[100];
int index;

int main()
{
    // printf("Gimme a string: ");
    // fgets(input, sizeof input, stdin); // you can also include --6-- in the int position here if input can be assumed, e.g. 'hello'

    // for (index = 0; input[index] != '\0'; index++)
    // {
    //     printf("%c\n", input[index]);
    // }
int i = 13;
printf("i = %d, !i = %d\n", i, !i);
printf("!0 = %d\n", !(0));


}